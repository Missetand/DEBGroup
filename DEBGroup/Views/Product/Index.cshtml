@model  DEBGroup.Models.Product.Index

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DisplayName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.AllProducts) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductID }) |
            @if (item.DetailsUrl != null && item.DetailsUrl.Trim() != "")
            {
                <a href="@item.DetailsUrl">deta..</a>
            }
            
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductID })
        </td>
    </tr>


}

@*@*</table>
<div class="form-horizontal">
    <h4>Currency</h4>
    <hr />

    <select name="Selectbox" multiple="">

        @foreach (var item in Model)
            {
            <option value="@item.ProductName">@item.ProductName</option>
        }
    </select>

</div>

@*<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>*@